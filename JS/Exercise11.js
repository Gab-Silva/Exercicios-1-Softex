let name = prompt("Whats your name?")
let idade = parseInt(prompt("How old are you?"))

if (idade >= 16) {
    document.write(name + ", you can vote.")
}
else {
    document.write = (name + ", you can't vote.")
}